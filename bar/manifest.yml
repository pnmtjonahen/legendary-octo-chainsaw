---
# comment in manifest
applications:
    - name : tjonahen-bartender
      no-route: true    
      buildpack: java_buildpack
      path: target/bartender.jar
      services:
        - myConfigServer
        - myServiceRegistry
        - myRabbitMQ
        - metrics-forwarder
