---
applications:
    - name : tjonahen-bartender
      buildpack: java_buildpack
      path: target/bartender.jar
      services:
        - myConfigServer
        - myServiceRegistry
        - myRabbitMQ
        - metrics-forwarder
