---
# comment in manifest
applications:
    - name : tjonahen-chef
      no-route: true    
      buildpack: java_buildpack
      path: target/chef.jar
      services:
        - myConfigServer
        - myServiceRegistry
        - myRabbitMQ
        - metrics-forwarder
        
