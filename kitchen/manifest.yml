---
# comment in manifest
applications:
    - name : tjonahen-chef
      buildpack: java_buildpack
      path: target/chef.jar
      services:
        - myConfigServer
        - myServiceRegistry
        - myRabbitMQ
        - metrics-forwarder
        
